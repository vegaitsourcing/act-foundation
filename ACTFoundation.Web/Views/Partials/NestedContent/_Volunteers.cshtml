@inherits UmbracoViewPage<ACTFoundation.Core.ViewModels.Shared.VolunteersViewModel>
@if( Model.Volunteers.Any() && Model.Volunteers != null) {
    foreach (var item in Model?.Volunteers)
    {
        Html.RenderPartial("~/Views/Partials/NestedContent/_VolunteersShared.cshtml", item);
    }
}